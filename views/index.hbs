<div class="jumbotron">
  <h1>Outlook Calendar Timeline</h1>
  <p class="lead">App that displays your Outlook calendar in a timeline format</p>
  {{#if user}}
    <p>Welcome {{user}}!</p>
  {{else}}
    <a class="btn btn-primary btn-large" href="{{signInUrl}}">Click here to login</a>
  {{/if}}
</div>